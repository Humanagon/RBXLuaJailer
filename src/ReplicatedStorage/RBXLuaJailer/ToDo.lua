--[[
	Make sure to make scripts need to be set to a privilege level in order set script sources since if the created script has no config, then it can likely do more than its creator.
--]]

return true